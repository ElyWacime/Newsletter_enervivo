def create_title(number):
    title = input(f"Enter the title (of article number {number}): ")
    return title

def create_description(number):
    description = input(f"Enter the description (of the article number {number}): ")
    return description

def create_image(number):
    image = input(f"Enter the url of the image for the article number {number}: ")
    return image